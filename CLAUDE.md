# プロジェクト常設ルール

情報処理安全確保支援士の学習支援Webアプリ。ユーザー本人が1人で使い、1人で保守する。

## 構成

素のHTML/CSS/JSの静的サイト（フレームワーク・ビルド不要を維持する）。GitHub Pages公開＋ローカルの `file://` 直開きの両方で動くこと。

- `index.html` — 画面骨格とscript読み込みのみ（薄く保つ）
- `css/app.css` — テーマ（オーロラ×グラスモーフィズム）
- `js/` — 機能ごとに分割: `app.js`(タブ・共通) / `state.js`(保存・同期・マージ) / `roadmap.js`(診断・計画) / `drill.js`(科目A-2ドリル) / `materials.js`(教材) / `exam-b.js`(科目B演習)
- `data/` — 問題・教材データ。**fetch()のJSONではなく `window.XXX =` 代入の .js ファイル**にする（file://で動かすため）
- `tools/` — データ生成用Pythonスクリプト（ユーザーが後日過去問を追加する用）
- `docs/trend-analysis.md` — 出題傾向分析。ロードマップの重要度ランク（S/A/B）の根拠
- 学習データは localStorage キー `sc-study-v1`。**構造を変えるときは必ず旧データからの移行処理を入れる**（ユーザーの学習履歴を消さない）
- クラウド同期: GitHub Gist（非公開、ファイル名 `sc-study-sync.json`）。トークン・Gist IDは別キー `sc-study-sync-v1` に保存し、**エクスポートや同期データに絶対に含めない**
- AI自動採点のClaude APIキーは別キー `sc-study-ai-v1`。同じく**エクスポート・同期・リポジトリに絶対に含めない**
- ロードマップ配分（`computeRoadmap`）は best-fit 方式＋科目A試験日の2週間前を中間締切とする2段階配分。この性質（最終週への積み残し防止・科目A演習が試験前に完了）を壊す変更は不可
- 進捗の取り消しは削除ではなく `{done:false, at}` の記録（tombstone）で表現する。端末間マージ（`mergeStates`）が「新しい操作が勝つ」で動くための前提。旧形式 `{done:true, doneAt}` も読めること

## 守るべき設計原則

1. **漏れゼロ原則**: ロードマップ生成（`computeRoadmap`）は全 UNITS を必ずどこかの週に割り当てる。時間不足でも単元を削らない（警告で対応）。この性質を壊す変更は不可
2. **学習履歴の完全性**: 単元の完了/取消・診断・設定変更はすべて `addLog` で記録する。新しい操作を追加したら必ずログも追加する
3. **出題傾向の根拠主義**: UNITS の trend 値や時間配分を変えるときは、先に `docs/trend-analysis.md` を更新して根拠を書いてから反映する
4. **著作権**: IPA公表の過去問はIPA利用規約に従い**出典明記の上で転載可**（例:「出典: 令和7年度 秋期 情報処理安全確保支援士試験 午前II 問1」。改変したら modified:true で明記）。解説・講評・教材はすべて自作。**IPA以外のサイト（過去問解説サイト等）からの転載は禁止**。レベル診断の問題はオリジナルを維持
5. UIは日本語。専門用語には初学者向けの補足を付ける

## ユーザーの前提

- セキュリティは初学者。目標は後期試験（科目B: 2027/3/16〜）
- 保守スキル: 単一HTML/JS、PowerShell、Python。凝った構成より読める・直せるコードを優先
